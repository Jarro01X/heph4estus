name: masscan
description: High-speed TCP port scanner
exec: ["masscan", "-iL", "{{input}}", "-oJ", "{{output}}", "--rate", "1000"]
input_type: target_list
output_ext: json
install_cmd: "apk add --no-cache masscan"
default_cpu: 256
default_memory: 512
timeout: 10m
tags: [scanner, network]
