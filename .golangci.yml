run:
  timeout: 3m

linters:
  disable-all: true
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gosimple
    - gomod

issues:
  exclude-dirs:
    - vendor
  exclude-generated: strict
